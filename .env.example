# 用户原始需求（2026-08-11）：以一个可独立部署的家庭节点承载计算、对象存储与内部路由。
# 只填写主机名，不包含协议、端口或路径。
IWEB_BASE_HOST=family.iweb.test
# 宿主侧发布端口（容器内 Kernel 恒绑 8080，Docker 负责映射）。
IWEB_HTTP_PORT=9010
# Upgrade rollback pins this variable to the previously accepted node image.
IWEB_IMAGE=ghcr.io/jixoai/openiweb:local
# rust-kernel-rustfs-storage §5.3（Codex R2 强化）：控制面调用只走容器内回环控制
# 监听器 http://127.0.0.1:7070；IWEB_CONTROL_ORIGIN 已废除——设置任何非默认值都会
# 使节点拒绝启动（fail-closed）。
# IWEB_CONTROL_ORIGIN=http://127.0.0.1:7070
# 每个 iweb 安装必须唯一，且在重启时保持不变。
CELLD_NODE=replace-with-a-unique-installation-id
IWEB_API_TOKEN=replace-with-a-long-random-api-token
# RustFS 兼容 MinIO 的根凭据变量名；节点内对象存储已是 RustFS（console 关闭）。
MINIO_ROOT_USER=iweb-root
MINIO_ROOT_PASSWORD=replace-with-a-long-random-password
# 存储引擎自适应 buffer profile 已在 entrypoint 无条件钉为 IndustrialIoT（稳态内存
# 平坦，决策 3 预算依据）；复测其它 profile 需改 entrypoint 并重建镜像。
# RUSTFS_BUFFER_PROFILE=IndustrialIoT
CELLD_S3_ACCESS_KEY=iweb-celld
CELLD_S3_SECRET_KEY=replace-with-a-long-random-secret
# 设为 1 时，容器启动会发布内置 Worker 的新版本。
IWEB_DEPLOY_ON_START=0
# Generic application execution remains closed until the image contains an
# independently verified sandbox-acceptance record. Setting this alone cannot
# enable publication.
IWEB_APPLICATION_PUBLICATION_ENABLED=0
